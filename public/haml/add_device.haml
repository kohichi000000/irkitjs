%section#add_device
	%ol
		%li
			%p 以下のclientkey,devicekey,deviceidをコピーして下さい
			%p<
				clientkey
				#display_clientkey.getDisplayKey
			%p<
				devicekey
				#display_devicekey.getDisplayKey
			%p<
				deviceid
				#display_deviceid.getDisplayKey
		%li<
			%p  追加したいデバイスでIRKitjsを開いて、「デバイスを追加する」ボタンをタップします。
		%li<
			%p コピーしたclientkey,devicekey,deviceidをペーストして、「デバイス登録」ボタンをタップします。	